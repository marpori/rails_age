<%= form_with(model: flintstones_character) do |form| %>
  <% if flintstones_character.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(flintstones_character.errors.count, "error") %> prohibited this flintstones_character from being saved:</h2>

      <ul>
        <% flintstones_character.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
